# Pandanet Client Assets

## Installed App Source

Current target bundle on this machine:

`/Applications/GoPanda2.app/Contents/Resources/app.asar`

Recommended clean source archive:

`/Applications/GoPanda2.app/Contents/Resources/original-app.asar`

## Purpose

This document should become the authoritative inventory of the Pandanet client assets relevant to theming.

The theming tool should preferably read from `original-app.asar` so each build starts from the stock client rather than a previously patched archive.

## Asset Inventory Checklist

For each themeable asset inside `app.asar`, capture:

- Internal ASAR path
- Semantic role: `board`, `stone-black`, `stone-white`
- File format
- Pixel dimensions
- Whether the asset is used directly or through CSS
- Whether it is a single image or part of a sprite sheet
- Any resizing or preprocessing needed when importing explicit asset files or Sabaki assets

## Confirmed Theme Assets

These files are the current primary theming targets discovered in the extracted archive:

| Semantic role | ASAR path | Expected size | Notes |
| --- | --- | --- | --- |
| `board` | `app/img/wood-board.jpg` | `150x150` stock asset | Stock client reference in CSS. The current utility does not overwrite this file; it patches `.goban` to point at a copied custom asset under `app/img/custom/`. |
| `stone-black` | `app/img/50/stone-black.png` | `50x50` | Stock client reference in JS/CSS. The current utility patches those references to a copied custom asset under `app/img/custom/`. |
| `stone-white` | `app/img/50/stone-white.png` | `50x50` | Stock client reference in JS/CSS. The current utility patches those references to a copied custom asset under `app/img/custom/`. |

## Board Vs Page Background

There are two different visual backgrounds in the client:

- The board texture inside `.goban` uses `app/img/wood-board.jpg`.
- The area around the board is styled by `.goban-page` and uses CSS radial gradients, not an image file.

## Grid Color

The board grid is not defined by an image asset.

- Grid lines are drawn on `grid-canvas` in `app/js/gopanda.js`.
- The `grid-canvas` class is reused outside the goban, including grid/list views elsewhere in the client.
- CSS experiments that target the board grid should scope to the goban canvas, for example `.goban > .grid-canvas`, instead of targeting `.grid-canvas` globally.
- The grid line color is hardcoded as `black` in the canvas rendering code.
- Coordinate labels are also canvas-drawn and use `rgba(0,0,0,0.5)`.
- Because the line color is baked into rendered canvas pixels, a CSS-only recolor has to use a filter on the already-drawn canvas instead of changing the original stroke color.

That means changing grid color is a code patching task, not an asset replacement task.
The current utility supports a CSS-only compromise through `--grid-rgba`: it appends a goban-scoped filter and opacity rule for `.goban > .grid-canvas`.

## Related Assets

These are not yet generated by the tool, but they are relevant to the complete visual theme:

| Asset | ASAR path | Size | Notes |
| --- | --- | --- | --- |
| Board shadow | `app/img/board-shadow.png` | `600x600` | Used by `.board-shadow` in `app/css/site.css`. |
| Stone shadow | `app/img/50/stone-shadow.png` | `50x50` | Preloaded by `gopanda.js`. |
| Black stone with shadow | `app/img/50/stone-black-w-shadow.png` | `56x56` | Used in CSS, likely for non-canvas contexts. |
| White stone with shadow | `app/img/50/stone-white-w-shadow.png` | `56x56` | Used in CSS, likely for non-canvas contexts. |
| Black variation stone | `app/img/50/variation-black.png` | `62x62` | Preloaded by `gopanda.js`. |
| White variation stone | `app/img/50/variation-white.png` | `62x62` | Preloaded by `gopanda.js`. |
| Both variation stone | `app/img/50/variation-both.png` | `62x62` | Preloaded by `gopanda.js`. |

## Extraction Notes

- Archive extracted from `/Applications/GoPanda2.app/Contents/Resources/app.asar` on April 18, 2026.
- Extraction command: `npm exec --package=@electron/asar asar -- extract app.asar extracted-app`
- Listing command: `npm exec --package=@electron/asar asar -- list app.asar`
- Code references found by search:
  - `app/css/site.css` references `wood-board.jpg`, `board-shadow.png`, `stone-black-w-shadow.png`, `stone-white-w-shadow.png`, `stone-black.png`, and `stone-white.png`.
  - `app/js/gopanda.js` preloads `stone-black.png`, `stone-white.png`, `stone-shadow.png`, `variation-black.png`, `variation-white.png`, and `variation-both.png`.
  - `app/css/site.css` sets `.goban { background: url("../img/wood-board.jpg") repeat; }`, which confirms the stock board texture is a DOM background rather than a canvas image.
  - `app/css/site.css` defines `.goban-page` with radial-gradient backgrounds, which confirms the page background around the board is separate from `wood-board.jpg`.
  - `app/js/gopanda.js` sets the grid canvas stroke color with `strokeStyle = "black"` and the coordinate label color with `fillStyle = "rgba(0,0,0,0.5)"`.

## Notes On Sabaki Compatibility

Sabaki themes are still useful as an import source because they package board and stone visuals plus CSS describing how they are used. Pandanet does not consume the Sabaki theme directly, so this project imports those assets, copies them into `app/img/custom/`, and patches Pandanet's CSS/JS references to point at them. This avoids rasterizing SVG themes before Electron renders them.

For stone scaling and offset, the tool currently supports a narrow subset of Sabaki CSS:

- `.shudan-stone-image.shudan-sign_1`
- `.shudan-stone-image.shudan-sign_-1`
- properties: `width`, `height`, `top`, `left`

When those are present with percentage values, the tool keeps the original stone asset, scales the imported values down to compensate for Shudan's default `calc(100% - .08em)` stone inset, patches CSS background sizing/positioning for non-canvas stone uses, and injects a small runtime script into the app so Electron adjusts canvas `drawImage()` calls for the custom stone files. This avoids rasterization and avoids fragile surgery inside Pandanet's minified JS bundle.

For fuzzy placement and last-move marker alignment, the runtime script uses two separate seams:

- it wraps `CanvasRenderingContext2D.prototype.drawImage` for the custom goban stone assets and records the final shifted stone center that was actually rendered
- it wraps `CanvasRenderingContext2D.prototype.arc` and applies that recorded offset to the next marker-sized arc on the same goban canvas context

That `arc()` hook is used instead of trying to override Pandanet's internal `v0(...)` marker function. In the extracted client, `v0(...)` is a local compiled binding rather than `window.v0`, so replacing `window.v0` has no effect on the actual marker path.

## Open Questions

- How should Sabaki stone assets be transformed into Pandanet's `-w-shadow` and `variation-*` derivatives?
- Does replacing only the base `50x50` stones leave any visible default assets in game lists or dialogs?
- Is `board-shadow.png` generic enough to preserve, or should it become part of the theme pipeline?
